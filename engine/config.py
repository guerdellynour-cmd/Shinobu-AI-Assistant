# These are fallback defaults only. The values actually used at runtime
# live in the "settings" table (engine/db.py) and can be changed from the
# Settings tab in the app without editing code.

ASSISTANT_NAME = 'Shinobu'
DEFAULT_VOICE_INDEX = 1
DEFAULT_RATE = 190